#pragma once

namespace zcm {

    /// Return the float epsilon.
    float epsilon() noexcept;

    /// Return 0.
    float zero() noexcept;

    /// Return 1.
    float one() noexcept;

    /// Return the pi constant.
    float pi() noexcept;

    /// Return pi * 2.
    float two_pi() noexcept;

    /// Return square root of pi.
    float root_pi() noexcept;

    /// Return pi / 2.
    float half_pi() noexcept;

    /// Return pi / 2 * 3.
    float three_over_two_pi() noexcept;

    /// Return pi / 4.
    float quarter_pi() noexcept;

    /// Return 1 / pi.
    float one_over_pi() noexcept;

    /// Return 1 / (pi * 2).
    float one_over_two_pi() noexcept;

    /// Return 2 / pi.
    float two_over_pi() noexcept;

    /// Return 4 / pi.
    float four_over_pi() noexcept;

    /// Return 2 / sqrt(pi).
    float two_over_root_pi() noexcept;

    /// Return 1 / sqrt(2).
    float one_over_root_two() noexcept;

    /// Return sqrt(pi / 2).
    float root_half_pi() noexcept;

    /// Return sqrt(2 * pi).
    float root_two_pi() noexcept;

    /// Return sqrt(ln(4)).
    float root_ln_four() noexcept;

    /// Return e constant.
    float e() noexcept;

    /// Return Euler's constant.
    float euler() noexcept;

    /// Return sqrt(2).
    float root_two() noexcept;

    /// Return sqrt(3).
    float root_three() noexcept;

    /// Return sqrt(5).
    float root_five() noexcept;

    /// Return ln(2).
    float ln_two() noexcept;

    /// Return ln(10).
    float ln_ten() noexcept;

    /// Return ln(ln(2)).
    float ln_ln_two() noexcept;

    /// Return 1 / 3.
    float third() noexcept;

    /// Return 2 / 3.
    float two_thirds() noexcept;

    /// Return the golden ratio constant.
    float golden_ratio() noexcept;

}
